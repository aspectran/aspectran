<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE aspectran PUBLIC "-//ASPECTRAN//DTD Aspectran Configuration 6.0//EN"
        "http://aspectran.github.io/dtd/aspectran-6.dtd">
<aspectran>

    <description>
        A collection of commands for controlling the Embedded Jetty Server.
    </description>

    <translet name="jetty">
        <description style="apon">
            |
            |{{underline}}To control Jetty, try one of the following commands:{{reset}}
            |   {{CYAN}}jetty start{{fg:reset}}  Runs a Jetty Server
            |   {{CYAN}}jetty stop{{fg:reset}}   Stops the currently running Jetty Server
        </description>
        <transform type="transform/text">
            <template style="apon">
                |
                |   {{YELLOW}}Jetty #{jetty.server^version} - #{jetty.server^state}{{reset}}
                |
            </template>
        </transform>
    </translet>

    <translet name="jetty start">
        <description style="apon">
            |
            |   {{cyan}}Starting the Jetty Server...{{reset}}
            |-----------------------------------------
        </description>
        <action bean="jetty.server" method="start"/>
        <transform type="transform/text">
            <template style="apon">
                |
                |   {{YELLOW}}Jetty #{jetty.server^version} - #{jetty.server^state}{{reset}}
                |   Web server started on localhost:%{jetty.server.port}
                |
            </template>
        </transform>
        <exception>
            <thrown type="java.net.BindException">
                <transform type="transform/text">
                    <template style="apon">
                        |
                        |   {{RED}}Jetty Server Error - Port %{jetty.server.port} already in use{{reset}}
                        |
                    </template>
                </transform>
            </thrown>
        </exception>
    </translet>

    <translet name="jetty stop">
        <description style="apon">
            |
            |   {{cyan}}Stopping the Jetty Server...{{reset}}
            |-----------------------------------------
        </description>
        <action bean="jetty.server" method="stop"/>
        <transform type="transform/text">
            <template style="apon">
                |
                |   {{YELLOW}}Jetty #{jetty.server^version} - #{jetty.server^state}{{reset}}
                |
            </template>
        </transform>
    </translet>

</aspectran>