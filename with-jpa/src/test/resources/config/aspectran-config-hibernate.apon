context: {
    rules: classpath:config/db-context.xml
    scan: [
        com.aspectran.jpa
    ]
    profiles: {
        base: [
            h2
            hibernate
        ]
        default: [
            dev
            test
        ]
    }
}
